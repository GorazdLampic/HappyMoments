# Temp files
tmpclaude-*
nul

# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude internal
.claude/

# Build output
dist/

# Android build artifacts (but keep the project structure)
android/app/build/
android/.gradle/
android/build/
android/local.properties

# Secrets & credentials
.env
.env.*
*.jks
*.keystore
google-services.json
.firebase/
.wrangler/

# App icons are the committed 3-7-8 "Nice Numbers" PNGs (source of truth).
# Do NOT ignore them — they must ship as-is, not be regenerated from the old
# smiling-3 icon.svg (which was overwriting the in-app/profile logo on builds).

# Legacy React Native app (top-level only)
/app/
_align2.mjs
_*.mjs
